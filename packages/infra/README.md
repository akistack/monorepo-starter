# packages/infra

This directory is for engineering infrastruture related packages (usually levelled as L0 or L1).

Example responsibilities typically included in infra packages:

- Shared configs (Typescript config, lint config, etc.)
- Observability (logging, tracing, metrics)
- Deployment and runtime utilities
- Scripts
