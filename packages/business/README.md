# packages/business

> Note: `business` is just an example name. `modules` or `features` would also be appropriate.

This directory is for business module / feature packages (usually levelled as L3).

Example responsibilities typically included in business packages:
- Application-specific use cases and workflows
- Feature modules and orchestration logic
- UI-agnostic application services
- Composition of domain, infra, and shared libs
