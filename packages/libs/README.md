# packages/libs

This directory is for shared libraries (usually levelled as L1).

Example responsibilities typically included in libs packages:

- Shared utilities and helpers
- Components
- Shared types and schemas (without domain or business semantics)
