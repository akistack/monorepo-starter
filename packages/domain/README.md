# packages/domain

This directory is for domain-related packages (usually levelled as L2).

Example responsibilities typically included in domain packages:

- Domain models (entities, value objects, aggregates)
- Domain services and business rules
- Domain events and policies
- Invariants and validation logic
- Domain interfaces (repositories, gateways, ports)
