import type { IConfig } from '../../autoinstallers/plugins/node_modules/rush-init-project-plugin';

const config: IConfig = {
  prompts: [],
  plugins: [],
  defaultProjectConfiguration: {
    tags: ['level-4', 'web', 'app'],
  },
};

export default config;
